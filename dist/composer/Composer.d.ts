import { ReactNode } from "react";
export interface ComposerProps {
    text?: string;
    body?: ReactNode;
}
export declare const Composer: ({ text }: ComposerProps) => import("react/jsx-runtime").JSX.Element;
