import { ReactNode } from "react";
export interface HeaderProps {
    text?: string;
    body?: ReactNode;
}
export declare const Header: ({ text }: HeaderProps) => import("react/jsx-runtime").JSX.Element;
